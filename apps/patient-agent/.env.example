# patient-agent 环境变量模板（第 6 周 Tool Calling）
# 复制为 .env 后按需填写；真实 Key 切勿提交到仓库。

# —— 模型（必填）——
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=
LLM_MODEL=deepseek-chat

# —— 可选：本机代理（空则直连）——
LLM_PROXY_HOST=
LLM_PROXY_PORT=7890

# —— 可选：Prompt 模板版本 ——
PROMPT_VERSION=v1
